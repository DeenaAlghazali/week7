const hashepassword = require('./hashepassword');
const signUp = require('./signUp');
const login = require('./login');
const makeCookie = require('./makeCookie');
const homePage = require('./homePage');

module.exports = {
  hashepassword,
  signUp,
  login,
  makeCookie,
  homePage,
}